
  try {
    Flint.packages["react-motion"] = require("react-motion")
  }
  catch(e) {
    console.log('Error running package!')
    console.error(e)
  };

  try {
    Flint.packages["mouse-event-offset"] = require("mouse-event-offset")
  }
  catch(e) {
    console.log('Error running package!')
    console.error(e)
  };

  try {
    Flint.packages["chroma-js"] = require("chroma-js")
  }
  catch(e) {
    console.log('Error running package!')
    console.error(e)
  };

  try {
    Flint.packages["document-offset"] = require("document-offset")
  }
  catch(e) {
    console.log('Error running package!')
    console.error(e)
  };

  try {
    Flint.packages["scroll"] = require("scroll")
  }
  catch(e) {
    console.log('Error running package!')
    console.error(e)
  };

  try {
    Flint.packages["history"] = require("history")
  }
  catch(e) {
    console.log('Error running package!')
    console.error(e)
  };

  try {
    Flint.packages["highlight.js"] = require("highlight.js")
  }
  catch(e) {
    console.log('Error running package!')
    console.error(e)
  };
